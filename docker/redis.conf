# Redis RDB Persistence Configuration
# 900초(15분) 내 1건 이상 변경 시 스냅샷
save 900 1
# 300초(5분) 내 10건 이상 변경 시 스냅샷
save 300 10
# 60초 내 10000건 이상 변경 시 스냅샷
save 60 10000

dbfilename dump.rdb
dir /data

# 메모리 설정
maxmemory 256mb
maxmemory-policy allkeys-lru

# 로깅
loglevel notice
